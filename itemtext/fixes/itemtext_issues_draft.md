# Draft `itemtext_issues.qmd` additions -- generated 2026-09-04

Draft only -- nothing here has been pasted into the live page
(`../../irw_site/itemtext_issues.qmd`, datapages/irw repo). Generated by
`draft_issues_qmd.R` from each batch's `provenance.csv`. Review the wording
before pasting: the sentences below are templated from structured provenance
fields and are deliberately conservative, so some will read as more alarming
than the underlying situation warrants.

The page is one YAML `issues` list inside an R `yaml.load(r"---( ... )---")`
block, rendered as a searchable table -- append the entries below before the
closing `)---")` and re-check that the YAML parses. Order does not matter; the
page sorts by table name. A table whose `uploaded` stamp is still blank gets no
entry until it ships, and `check_issues_page.R` will re-report it once it does.

Source batches: itemtables/batch_022, itemtables/batch_023

## `promis1wave1_anger` (batch_022)

<!-- mapping_basis=data_labels text_source=study_materials source=doi:10.7910/DVN/0NGAKG (Appendix A - PROMIS Data Code Book, codebook.doc; Cella, David, Northwestern University; CC0 1.0) -->
<!-- full note: Item text, section prompts and response options come from the study's own PROMIS Wave 1 data codebook (Appendix A), which ships with the Harvard Dataverse deposit under CC0 1.0. The codebook lists each item by the exact variable name the response table uses, followed by its item context, stem and numbered options, so the code->text and resp->option_text mapping is a direct read of the study's own labels. The .doc was converted to .docx and parsed with textutil; the deposit's own web download is guestbook-gated, so the file was supplied manually. Cross-checks: all 616 item codes across the seven tables resolve in the codebook; every table's items fall entirely inside the expected codebook section, at exactly 56 items per section; and validate_items.R reports item-set and resp-set matching live data exactly for all seven tables. ONE ITEM SCORES AGAINST THE BANK. EDANG14 ('It was difficult to let people know I was angry', printed 1=Always through 5=Never) correlates -0.285 with the other 55 items, the only negative in the table. Its labels were checked rather than assumed and they are right as printed: under them 51.1% of respondents answered 'Never' and 1.7% 'Always' (mean 4.17), which is what a general sample should look like, whereas a flipped ladder would claim 51% always found it difficult to express anger. Its two siblings on the identical printed ladder confirm it independently -- EDANG19 'I controlled my angry feelings' and EDANG53 'able to calm my feelings' put 72.9% and 78.8% at 'Always/Often', and both correlate positively (+0.167, +0.226). The negative sign is a property of the phrasing, not of the coding: a rarely-angry respondent answers EDANG14 'Never difficult' (high) because the occasion seldom arises, while answering EDANG01 'When I was frustrated, I let it show' as 'Never' (low), so anger frequency drives the two in opposite coded directions -- which is also why r(EDANG14, EDANG01) is -0.196 while r(EDANG01, EDANG42) is +0.407. Note this item is absent from the official PROMIS calibrated anger bank, as are both its siblings. -->

```yaml
- table: promis1wave1_anger
  issue: |-
    Anchor direction is not uniform across items: 3 of the 56 items print a descending run (e.g. 5 = Never ... 1 = Always) and the rest ascend. option_text carries the label the codebook gives for each level, so read option_text rather than assuming a direction for resp. One item runs against the rest: EDANG14 ('It was difficult to let people know I was angry') is keyed 1=Always to 5=Never, so a high resp means the respondent found it easy to express anger. It is the only item correlating negatively with the other 55, and that is the phrasing rather than a coding error -- a respondent who is rarely angry answers 'Never difficult' at the high end. Do not sum it with the rest without reversing it first.
```

## `promis1wave1_anxiety` (batch_022)

<!-- mapping_basis=data_labels text_source=study_materials source=doi:10.7910/DVN/0NGAKG (Appendix A - PROMIS Data Code Book, codebook.doc; Cella, David, Northwestern University; CC0 1.0) -->
<!-- full note: Item text, section prompts and response options come from the study's own PROMIS Wave 1 data codebook (Appendix A), which ships with the Harvard Dataverse deposit under CC0 1.0. The codebook lists each item by the exact variable name the response table uses, followed by its item context, stem and numbered options, so the code->text and resp->option_text mapping is a direct read of the study's own labels. The .doc was converted to .docx and parsed with textutil; the deposit's own web download is guestbook-gated, so the file was supplied manually. Cross-checks: all 616 item codes across the seven tables resolve in the codebook; every table's items fall entirely inside the expected codebook section, at exactly 56 items per section; and validate_items.R reports item-set and resp-set matching live data exactly for all seven tables. -->

```yaml
- table: promis1wave1_anxiety
  issue: |-
    All 56 items print an ascending option run (1 = Never ... 5 = Always) exactly as option_text records.
```

## `promis1wave1_depression` (batch_022)

<!-- mapping_basis=data_labels text_source=study_materials source=doi:10.7910/DVN/0NGAKG (Appendix A - PROMIS Data Code Book, codebook.doc; Cella, David, Northwestern University; CC0 1.0) -->
<!-- full note: Item text, section prompts and response options come from the study's own PROMIS Wave 1 data codebook (Appendix A), which ships with the Harvard Dataverse deposit under CC0 1.0. The codebook lists each item by the exact variable name the response table uses, followed by its item context, stem and numbered options, so the code->text and resp->option_text mapping is a direct read of the study's own labels. The .doc was converted to .docx and parsed with textutil; the deposit's own web download is guestbook-gated, so the file was supplied manually. Cross-checks: all 616 item codes across the seven tables resolve in the codebook; every table's items fall entirely inside the expected codebook section, at exactly 56 items per section; and validate_items.R reports item-set and resp-set matching live data exactly for all seven tables. -->

```yaml
- table: promis1wave1_depression
  issue: |-
    All 56 items print an ascending option run (1 = Never ... 5 = Always) exactly as option_text records.
```

## `promis1wave1_fatigue` (batch_022)

<!-- mapping_basis=data_labels text_source=study_materials source=doi:10.7910/DVN/0NGAKG (Appendix A - PROMIS Data Code Book, codebook.doc; Cella, David, Northwestern University; CC0 1.0) -->
<!-- full note: Item text, section prompts and response options come from the study's own PROMIS Wave 1 data codebook (Appendix A), which ships with the Harvard Dataverse deposit under CC0 1.0. The codebook lists each item by the exact variable name the response table uses, followed by its item context, stem and numbered options, so the code->text and resp->option_text mapping is a direct read of the study's own labels. The .doc was converted to .docx and parsed with textutil; the deposit's own web download is guestbook-gated, so the file was supplied manually. Cross-checks: all 616 item codes across the seven tables resolve in the codebook; every table's items fall entirely inside the expected codebook section, at exactly 56 items per section; and validate_items.R reports item-set and resp-set matching live data exactly for all seven tables. Two codebook sections are represented here, Fatigue - Fatigue Experience and Fatigue - Fatigue Impact, carried as separate section_id values. -->

```yaml
- table: promis1wave1_fatigue
  issue: |-
    Anchor direction is not uniform across items: 17 of the 112 items print a descending run (e.g. 5 = Never ... 1 = Always) and the rest ascend. option_text carries the label the codebook gives for each level, so read option_text rather than assuming a direction for resp.
```

## `promis1wave1_pain` (batch_022)

<!-- mapping_basis=data_labels text_source=study_materials source=doi:10.7910/DVN/0NGAKG (Appendix A - PROMIS Data Code Book, codebook.doc; Cella, David, Northwestern University; CC0 1.0) -->
<!-- full note: Item text, section prompts and response options come from the study's own PROMIS Wave 1 data codebook (Appendix A), which ships with the Harvard Dataverse deposit under CC0 1.0. The codebook lists each item by the exact variable name the response table uses, followed by its item context, stem and numbered options, so the code->text and resp->option_text mapping is a direct read of the study's own labels. The .doc was converted to .docx and parsed with textutil; the deposit's own web download is guestbook-gated, so the file was supplied manually. Cross-checks: all 616 item codes across the seven tables resolve in the codebook; every table's items fall entirely inside the expected codebook section, at exactly 56 items per section; and validate_items.R reports item-set and resp-set matching live data exactly for all seven tables. Three codebook sections are represented here, Pain - Behavior, Pain - Interference and Pain - Quality, carried as separate section_id values. Some codebook option sets are 6 wide where live resp has only 5 levels. Which of two mechanisms applies was settled per item by the respondent count. Where there is no n deficit relative to sibling items, those respondents are present and the two bottom categories share live level 1, so option_text for level 1 joins both codebook labels with ' / ' (89 items: 'Had no pain / Never' and 'Not sure if I had this type of pain / Did not have this type of pain'; PAINBE 6-option median n=2503 and PAINQU 6-option median n=2642 against 2647/2546 for their 5-option siblings). Where there is a clear deficit, the extra category is absent from the live data and levels 1-5 map straight onto keys 1-5 (1 items). For PAININ21 the absent category is the escape option 'Does not apply to me' (n=1980 against a 2647 median for its 5-option siblings). -->

```yaml
- table: promis1wave1_pain
  issue: |-
    89 of the 168 items have a level 1 that covers two codebook categories at once, so its option_text joins both labels with ' / '. Anchor direction is not uniform across items: 2 of the 168 items print a descending run (e.g. 5 = Never ... 1 = Always) and the rest ascend. option_text carries the label the codebook gives for each level, so read option_text rather than assuming a direction for resp.
```

## `promis1wave1_physicalfunction` (batch_022)

<!-- mapping_basis=data_labels text_source=study_materials source=doi:10.7910/DVN/0NGAKG (Appendix A - PROMIS Data Code Book, codebook.doc; Cella, David, Northwestern University; CC0 1.0) -->
<!-- full note: Item text, section prompts and response options come from the study's own PROMIS Wave 1 data codebook (Appendix A), which ships with the Harvard Dataverse deposit under CC0 1.0. The codebook lists each item by the exact variable name the response table uses, followed by its item context, stem and numbered options, so the code->text and resp->option_text mapping is a direct read of the study's own labels. The .doc was converted to .docx and parsed with textutil; the deposit's own web download is guestbook-gated, so the file was supplied manually. Cross-checks: all 616 item codes across the seven tables resolve in the codebook; every table's items fall entirely inside the expected codebook section, at exactly 56 items per section; and validate_items.R reports item-set and resp-set matching live data exactly for all seven tables. All items sit in Physical Function - Part III. Anchor sets are mixed: 39 items use a 5-wide difficulty run, 12 are yes/no, and 5 are 6 wide. Some codebook option sets are 6 wide where live resp has only 5 levels. Which of two mechanisms applies was settled per item by the respondent count. Where there is no n deficit relative to sibling items, those respondents are present and the two bottom categories share live level 1, so option_text for level 1 joins both codebook labels with ' / ' (0 items: 'Had no pain / Never' and 'Not sure if I had this type of pain / Did not have this type of pain'; PAINBE 6-option median n=2503 and PAINQU 6-option median n=2642 against 2647/2546 for their 5-option siblings). Where there is a clear deficit, the extra category is absent from the live data and levels 1-5 map straight onto keys 1-5 (5 items). For PFC1-PFC5 the absent category is the top option 'Very easy': those five items run n=1069-2145 against a 2535 median for the other 51 items, and the deficit tracks item easiness (Spearman ~ -0.8; PFC1 'open a previously opened jar' has the largest deficit at n=1069 and the highest mean resp at 4.48), which is what dropping the easiest response produces and what a category collapse would not. Independent cross-check: item text extracted from the official PROMIS item-bank PDFs (promis_compiled.pdf) covers 26 of the 56 items here and agrees with the shipped text on 25. The exception is PFC43, where the codebook prints 'Are you able to do use your hands' against the instrument's 'Are you able to use your hands' -- a dittography in the codebook. The codebook wording is shipped verbatim rather than silently corrected, since it is the study's own document and what was administered cannot be confirmed from the deposit. -->

```yaml
- table: promis1wave1_physicalfunction
  issue: |-
    Anchor sets are mixed within the table: 39 items use a 5-level difficulty run, 12 are binary, and 5 run 1-5 over a 6-level codebook scale whose top option is absent from the data. Anchor direction is not uniform across items: 49 of the 56 items print a descending run (e.g. 5 = Never ... 1 = Always) and the rest ascend. option_text carries the label the codebook gives for each level, so read option_text rather than assuming a direction for resp.
```

## `promis1wave1_social` (batch_022)

<!-- mapping_basis=data_labels text_source=study_materials source=doi:10.7910/DVN/0NGAKG (Appendix A - PROMIS Data Code Book, codebook.doc; Cella, David, Northwestern University; CC0 1.0) -->
<!-- full note: Item text, section prompts and response options come from the study's own PROMIS Wave 1 data codebook (Appendix A), which ships with the Harvard Dataverse deposit under CC0 1.0. The codebook lists each item by the exact variable name the response table uses, followed by its item context, stem and numbered options, so the code->text and resp->option_text mapping is a direct read of the study's own labels. The .doc was converted to .docx and parsed with textutil; the deposit's own web download is guestbook-gated, so the file was supplied manually. Cross-checks: all 616 item codes across the seven tables resolve in the codebook; every table's items fall entirely inside the expected codebook section, at exactly 56 items per section; and validate_items.R reports item-set and resp-set matching live data exactly for all seven tables. Two codebook sections are represented here, Social Role - Performance and Social Role - Satisfaction, carried as separate section_id values. The codebook misprints one option key on 19 Social Role - Performance items, printing '3 = Sometimes3 = Often1 = Always' where the descending ladder requires 2 for 'Often'; 17 sibling items in the same document print '2 = Often' for the identical anchor set, and 2 is the only value missing from an otherwise contiguous 5..1 run, so the key was read as 2. -->

```yaml
- table: promis1wave1_social
  issue: |-
    Anchor direction is not uniform across items: 48 of the 112 items print a descending run (e.g. 5 = Never ... 1 = Always) and the rest ascend. option_text carries the label the codebook gives for each level, so read option_text rather than assuming a direction for resp.
```

## `promis1wave1_cesd` (batch_022)

<!-- mapping_basis=data_labels text_source=study_materials source=doi:10.7910/DVN/0NGAKG (Appendix A - PROMIS Data Code Book, codebook.doc; Cella, David, Northwestern University; CC0 1.0) -->
<!-- full note: Item text and options come from the study's own PROMIS Wave 1 codebook (Appendix A, doi:10.7910/DVN/0NGAKG, CC0 1.0), which names each of CESD1-CESD20 and pairs it with its stem and its four numbered options, so the mapping is a direct read of the study's own labels. This scale sits in the codebook's legacy-items tables rather than the PROMIS bank sections, so it has its own re-derivation script. That layout differs in three ways the parser handles explicitly: rows repeat an instruction sentence before the stem, trailing Domain / Sub-Domain columns run into the last option label, and a code can also appear in the scoring appendix. Two traps were hit and handled: 'CESD16' also occurs in the scoring appendix ('CESD16 need to be recoded'), so rows are anchored on the code followed by the instruction sentence; and CESD20, the last row of the block, is not followed by another CESD code, so its option run continued into the next scale and had to be cut where the key run breaks. All 20 items share one response set, which rederive_cesd.py asserts. Independent corroboration of the code-to-text tie, using the data rather than the codebook: the canonical CES-D words items 4, 8, 12 and 16 positively and the other 16 negatively, the shipped stems put the positive wording at exactly those four numbers ('I felt that I was just as good as other people', 'I felt hopeful about the future', 'I was happy', 'I enjoyed life'), and in the live data those four are the only items that correlate negatively with the rest -- mean r against the other 19 of -0.29 to -0.13 against +0.22 to +0.34 for all sixteen others, with no overlap between the two groups. The shared instruction sentence is carried in instructions; the codebook gives no separate item context, so section_prompt is empty. -->

```yaml
- table: promis1wave1_cesd
  issue: |-
    The 20-item CES-D. resp runs 0-3 from 'Rarely or none of the time (Less than 1 day)' to 'Most or all of the time (5-7 days)', one shared response set across every item. Items 4, 8, 12 and 16 are positively worded and so score in reverse of the other sixteen.
```

## `promis1wave1_haq` (batch_022)

<!-- mapping_basis=data_labels text_source=study_materials source=doi:10.7910/DVN/0NGAKG (Appendix A - PROMIS Data Code Book, codebook.doc; Cella, David, Northwestern University; CC0 1.0) -->
<!-- full note: Item text and options come from the study's own PROMIS Wave 1 codebook (Appendix A, doi:10.7910/DVN/0NGAKG, CC0 1.0), which names each of the 24 codes and pairs it with its stem and numbered options, so the mapping is a direct read of the study's own labels. This scale sits in the codebook's legacy-items tables rather than the PROMIS bank sections, so it has its own re-derivation script. That layout differs in three ways the parser handles explicitly: rows repeat an instruction sentence before the stem, trailing Domain / Sub-Domain columns run into the last option label, and a code can also appear in the scoring appendix. HAQ also needed its own parser because its codes end in a letter (HAQ1a, HAQ3c) while the bank sections' codes end in a digit, and because HAQ9-HAQ12 print their options in descending key order ('1 = Yes0 = No') where the difficulty items ascend, so option runs are read as monotone in either direction. Two sub-blocks are carried as separate section_id values: 20 items asking how much difficulty an activity caused over the past week, on a 0-3 run from 'Without any difficulty' to 'Unable to do', and 4 items asking whether an aid or another person is usually needed, yes/no. Corroboration, reported rather than relied on: the numeric prefix marks the HAQ's eight activity categories, and items sharing a prefix cohere more than items across two (within-category mean r=0.582 over 16 pairs against 0.499 over 174), which is the direction the category structure predicts, though the ranges overlap because the HAQ disability index is strongly unidimensional. -->

```yaml
- table: promis1wave1_haq
  issue: |-
    The HAQ disability items. 20 items ask how much difficulty an activity caused over the past week, resp 0-3 from 'Without any difficulty' to 'Unable to do'; the other 4 (HAQ9-HAQ12) ask whether an aid or another person is usually needed and are yes/no, resp 0-1. The numeric prefix marks the instrument's activity category, so HAQ1a and HAQ1b are two items about the same category.
```

## `ecps_sahm_2024_ia` (batch_022)

<!-- mapping_basis=paper_order text_source=study_materials source=osf.io/36tsd 'Copy of survey.pdf' (the administered Qualtrics form, in the project's Final Data Set folder) with 'Measured Variables.xlsx'; osf.io/vxuf4 'MainStudy_CriticalMeasures.xlsx' -- COVIDiSTRESS Global Survey Round II, CC-BY 4.0. Data file Final_COVIDiSTRESS_Vol2_cleaned.csv, obtained via osf.io/n26mb. Paper: Blackburn, Vestergren et al. (2022), Scientific Data 9, 331, doi:10.1038/s41597-022-01383-6 -->
<!-- full note: SOURCE CORRECTION, raised and applied under #1831 on 2026-09-04. The IRW dictionary attributed all eleven ecps_sahm_2024_* tables to Sahm, Burghart & Mier's German ERQ-S validation (doi:10.17605/OSF.IO/N26MB), describing each as a subscale of that instrument. That was wrong for ten of the eleven: the data are the COVIDiSTRESS Global Survey Round II, whose cleaned file is hosted in the ERQ-S OSF project alongside that study's own files, with that project's README pointing to osf.io/36tsd for the file's description -- which is how the mix-up arose. Every item code in all eleven tables is a column of that file, which rederive_ecps.py re-checks. The exception is ecps_sahm_2024_emotion, which genuinely is the ERQ short form: COVIDiSTRESS II included it as a block, and that is the subsample Sahm et al. validated. Item text is tied to item codes by the instrument's listing order within each block, with block item counts matching the live tables exactly. The information acquisition block lists 7 sources and the table has 7 items, the seventh being 'Other, please specify'. OPTION LABELS COME FROM THE ADMINISTERED FORM, not the questionnaire workbooks. The two workbooks on OSF are registration-stage documents and their response designs do not describe what was administered: they give information acquisition as a 1-7 slider where the data run 1-10, and the agreement scales as 1-6 where the data run 1-7 or 0-6. 'Copy of survey.pdf', in the Round II project's own Final Data Set folder, is the Qualtrics form and prints every ladder verbatim. It settled two things the workbooks got wrong: the agreement ladder carries a NEUTRAL midpoint the workbooks omitted, so a six-anchor list is really seven options; and the compliance items are past tense. Every live resp level in all eight tables maps onto a position in its own item's ladder, with no level left over. The form is authoritative but its print view is partial -- it renders one statement per randomised misperception block and two rows of each norm matrix -- so 190 shipped strings verify against it and 16 against the workbooks, 0 against neither, which rederive_ecps.py reports per run. Positions the form left without a verbal label ship empty rather than invented. Its ladder is a 10-point slider printed 'Not at all | 1 2 3 4 5 6 7 8 | Frequently', which is what the live 1-10 range is; only the two ends carry a verbal label, so 14 of the 70 option cells are labelled and 56 are the form's own unlabelled positions. The survey ran in 47 languages across 179 countries, so no single administered language exists to name; the shipped text is the English master form and the language column is omitted rather than given a value wrong for most respondents. Raised on #1831 as a case #1777 does not cover.  -->

```yaml
- table: ecps_sahm_2024_ia
  issue: |-
    Item text and the response ladder are the administered English wording of the COVIDiSTRESS Round II form. resp runs 1-10 on a slider whose ends only are labelled, 1 = 'Not at all' and 10 = 'Frequently'; the eight intermediate positions had no verbal label and so carry none here. Items are tied to codes by the form's listing order.
```

## `ecps_sahm_2024_distrust` (batch_022)

<!-- mapping_basis=paper_order text_source=study_materials source=osf.io/36tsd 'Copy of survey.pdf' (the administered Qualtrics form, in the project's Final Data Set folder) with 'Measured Variables.xlsx'; osf.io/vxuf4 'MainStudy_CriticalMeasures.xlsx' -- COVIDiSTRESS Global Survey Round II, CC-BY 4.0. Data file Final_COVIDiSTRESS_Vol2_cleaned.csv, obtained via osf.io/n26mb. Paper: Blackburn, Vestergren et al. (2022), Scientific Data 9, 331, doi:10.1038/s41597-022-01383-6 -->
<!-- full note: SOURCE CORRECTION, raised and applied under #1831 on 2026-09-04. The IRW dictionary attributed all eleven ecps_sahm_2024_* tables to Sahm, Burghart & Mier's German ERQ-S validation (doi:10.17605/OSF.IO/N26MB), describing each as a subscale of that instrument. That was wrong for ten of the eleven: the data are the COVIDiSTRESS Global Survey Round II, whose cleaned file is hosted in the ERQ-S OSF project alongside that study's own files, with that project's README pointing to osf.io/36tsd for the file's description -- which is how the mix-up arose. Every item code in all eleven tables is a column of that file, which rederive_ecps.py re-checks. The exception is ecps_sahm_2024_emotion, which genuinely is the ERQ short form: COVIDiSTRESS II included it as a block, and that is the subsample Sahm et al. validated. Three blocks are represented as separate section_id values. The misperception block is an explicit numbering match rather than an order inference: the workbooks number its 6 statements B.1.1 to B.3.2 and the codes run misperception1_1 to misperception3_2, so block and position are named at each end. Conspiratorial thinking (4) and anti-expert sentiment (3) are listing order, counts matching exactly. OPTION LABELS COME FROM THE ADMINISTERED FORM, not the questionnaire workbooks. The two workbooks on OSF are registration-stage documents and their response designs do not describe what was administered: they give information acquisition as a 1-7 slider where the data run 1-10, and the agreement scales as 1-6 where the data run 1-7 or 0-6. 'Copy of survey.pdf', in the Round II project's own Final Data Set folder, is the Qualtrics form and prints every ladder verbatim. It settled two things the workbooks got wrong: the agreement ladder carries a NEUTRAL midpoint the workbooks omitted, so a six-anchor list is really seven options; and the compliance items are past tense. Every live resp level in all eight tables maps onto a position in its own item's ladder, with no level left over. The form is authoritative but its print view is partial -- it renders one statement per randomised misperception block and two rows of each norm matrix -- so 190 shipped strings verify against it and 16 against the workbooks, 0 against neither, which rederive_ecps.py reports per run. Positions the form left without a verbal label ship empty rather than invented. All three blocks share the 7-point agreement ladder, so all 91 option cells are labelled. The survey ran in 47 languages across 179 countries, so no single administered language exists to name; the shipped text is the English master form and the language column is omitted rather than given a value wrong for most respondents. Raised on #1831 as a case #1777 does not cover.  -->

```yaml
- table: ecps_sahm_2024_distrust
  issue: |-
    Item text and the 7-point agreement ladder (Strongly disagree, Disagree, Slightly disagree, Neutral, Slightly agree, Agree, Strongly agree) are the administered English wording. The 6 misperception items are matched by the form's own B.x.y numbering; the 4 conspiratorial-thinking and 3 anti-expert items by listing order. Note on the table name: the `ecps_sahm_2024_` prefix points at Sahm et al.'s German Emotion Regulation Questionnaire validation, which is not what this table is. These responses are the COVIDiSTRESS Global Survey Round II; its cleaned data file is hosted inside that study's OSF project, which is how the two came to be conflated. The name is kept because renaming a published table breaks every existing reference to it.
```

## `ecps_sahm_2024_dtl` (batch_022)

<!-- mapping_basis=paper_order text_source=study_materials source=osf.io/36tsd 'Copy of survey.pdf' (the administered Qualtrics form, in the project's Final Data Set folder) with 'Measured Variables.xlsx'; osf.io/vxuf4 'MainStudy_CriticalMeasures.xlsx' -- COVIDiSTRESS Global Survey Round II, CC-BY 4.0. Data file Final_COVIDiSTRESS_Vol2_cleaned.csv, obtained via osf.io/n26mb. Paper: Blackburn, Vestergren et al. (2022), Scientific Data 9, 331, doi:10.1038/s41597-022-01383-6 -->
<!-- full note: SOURCE CORRECTION, raised and applied under #1831 on 2026-09-04. The IRW dictionary attributed all eleven ecps_sahm_2024_* tables to Sahm, Burghart & Mier's German ERQ-S validation (doi:10.17605/OSF.IO/N26MB), describing each as a subscale of that instrument. That was wrong for ten of the eleven: the data are the COVIDiSTRESS Global Survey Round II, whose cleaned file is hosted in the ERQ-S OSF project alongside that study's own files, with that project's README pointing to osf.io/36tsd for the file's description -- which is how the mix-up arose. Every item code in all eleven tables is a column of that file, which rederive_ecps.py re-checks. The exception is ecps_sahm_2024_emotion, which genuinely is the ERQ short form: COVIDiSTRESS II included it as a block, and that is the subsample Sahm et al. validated. Two blocks are represented as separate section_id values, resilience and intolerance of uncertainty. OPTION LABELS COME FROM THE ADMINISTERED FORM, not the questionnaire workbooks. The two workbooks on OSF are registration-stage documents and their response designs do not describe what was administered: they give information acquisition as a 1-7 slider where the data run 1-10, and the agreement scales as 1-6 where the data run 1-7 or 0-6. 'Copy of survey.pdf', in the Round II project's own Final Data Set folder, is the Qualtrics form and prints every ladder verbatim. It settled two things the workbooks got wrong: the agreement ladder carries a NEUTRAL midpoint the workbooks omitted, so a six-anchor list is really seven options; and the compliance items are past tense. Every live resp level in all eight tables maps onto a position in its own item's ladder, with no level left over. The form is authoritative but its print view is partial -- it renders one statement per randomised misperception block and two rows of each norm matrix -- so 190 shipped strings verify against it and 16 against the workbooks, 0 against neither, which rederive_ecps.py reports per run. Positions the form left without a verbal label ship empty rather than invented. This table was held out of the batch until the administered form was found, because the workbooks list only 4 uncertainty statements against codes uncertainty_1-5. The form lists 5, the extra one being 'I should be able to organize everything in advance' in second position, which is what unblocked it. The two blocks use different ladders and the data agree: resilience runs 1-7 on the agreement ladder and uncertainty 1-5 on 'Not at all characteristic of me' to 'Entirely characteristic of me'. All 67 option cells are labelled. The survey ran in 47 languages across 179 countries, so no single administered language exists to name; the shipped text is the English master form and the language column is omitted rather than given a value wrong for most respondents. Raised on #1831 as a case #1777 does not cover.  -->

```yaml
- table: ecps_sahm_2024_dtl
  issue: |-
    Item text and both ladders are the administered English wording. The table pools two different scales: the 6 resilience items run 1-7 on an agreement ladder, and the 5 uncertainty items run 1-5 on a 'characteristic of me' ladder, so resp is not comparable across the two blocks -- read section_id and option_text rather than assuming one scale. Note on the table name: the `ecps_sahm_2024_` prefix points at Sahm et al.'s German Emotion Regulation Questionnaire validation, which is not what this table is. These responses are the COVIDiSTRESS Global Survey Round II; its cleaned data file is hosted inside that study's OSF project, which is how the two came to be conflated. The name is kept because renaming a published table breaks every existing reference to it.
```

## `ecps_sahm_2024_identity` (batch_022)

<!-- mapping_basis=paper_order text_source=study_materials source=osf.io/36tsd 'Copy of survey.pdf' (the administered Qualtrics form, in the project's Final Data Set folder) with 'Measured Variables.xlsx'; osf.io/vxuf4 'MainStudy_CriticalMeasures.xlsx' -- COVIDiSTRESS Global Survey Round II, CC-BY 4.0. Data file Final_COVIDiSTRESS_Vol2_cleaned.csv, obtained via osf.io/n26mb. Paper: Blackburn, Vestergren et al. (2022), Scientific Data 9, 331, doi:10.1038/s41597-022-01383-6 -->
<!-- full note: SOURCE CORRECTION, raised and applied under #1831 on 2026-09-04. The IRW dictionary attributed all eleven ecps_sahm_2024_* tables to Sahm, Burghart & Mier's German ERQ-S validation (doi:10.17605/OSF.IO/N26MB), describing each as a subscale of that instrument. That was wrong for ten of the eleven: the data are the COVIDiSTRESS Global Survey Round II, whose cleaned file is hosted in the ERQ-S OSF project alongside that study's own files, with that project's README pointing to osf.io/36tsd for the file's description -- which is how the mix-up arose. Every item code in all eleven tables is a column of that file, which rederive_ecps.py re-checks. The exception is ecps_sahm_2024_emotion, which genuinely is the ERQ short form: COVIDiSTRESS II included it as a block, and that is the subsample Sahm et al. validated. Item text is tied to item codes by the instrument's listing order within each block, with block item counts matching the live tables exactly. The identity block lists 4 statements and the table has 4 items. The wording is corroborated by a second document: the block appears in both workbooks and their 5 lines agree word for word. OPTION LABELS COME FROM THE ADMINISTERED FORM, not the questionnaire workbooks. The two workbooks on OSF are registration-stage documents and their response designs do not describe what was administered: they give information acquisition as a 1-7 slider where the data run 1-10, and the agreement scales as 1-6 where the data run 1-7 or 0-6. 'Copy of survey.pdf', in the Round II project's own Final Data Set folder, is the Qualtrics form and prints every ladder verbatim. It settled two things the workbooks got wrong: the agreement ladder carries a NEUTRAL midpoint the workbooks omitted, so a six-anchor list is really seven options; and the compliance items are past tense. Every live resp level in all eight tables maps onto a position in its own item's ladder, with no level left over. The form is authoritative but its print view is partial -- it renders one statement per randomised misperception block and two rows of each norm matrix -- so 190 shipped strings verify against it and 16 against the workbooks, 0 against neither, which rederive_ecps.py reports per run. Positions the form left without a verbal label ship empty rather than invented. The _0neutral decode: the cleaned file carries each of these items twice, as *_0neutral and *_midneutral, and IRW uses the _0neutral copy. Cross-tabulating the pair recovers the scale exactly -- 32 of the 33 paired items give 0->4, 1->1, 2->2, 3->3, 4->5, 5->6, 6->7, so the midpoint is pulled out to 0 and the six named anchors stay at 1-6. The exception is the single willingness item, a 5-point scale giving 0->3, 1->1, 2->2, 3->4, 4->5. Re-run per item by rederive_ecps.py. So all 28 option cells are labelled: 0 = Neutral and 1-6 the six named anchors. The survey ran in 47 languages across 179 countries, so no single administered language exists to name; the shipped text is the English master form and the language column is omitted rather than given a value wrong for most respondents. Raised on #1831 as a case #1777 does not cover.  -->

```yaml
- table: ecps_sahm_2024_identity
  issue: |-
    Item text is the administered English wording. resp 0-6 is a 7-point agreement scale whose midpoint the cleaned data recodes to 0: 0 = Neutral, then 1 = Strongly disagree through 6 = Strongly agree. Note that 0 is therefore the MIDDLE of the scale, not its bottom. Note on the table name: the `ecps_sahm_2024_` prefix points at Sahm et al.'s German Emotion Regulation Questionnaire validation, which is not what this table is. These responses are the COVIDiSTRESS Global Survey Round II; its cleaned data file is hosted inside that study's OSF project, which is how the two came to be conflated. The name is kept because renaming a published table breaks every existing reference to it.
```

## `ecps_sahm_2024_moral` (batch_022)

<!-- mapping_basis=paper_order text_source=study_materials source=osf.io/36tsd 'Copy of survey.pdf' (the administered Qualtrics form, in the project's Final Data Set folder) with 'Measured Variables.xlsx'; osf.io/vxuf4 'MainStudy_CriticalMeasures.xlsx' -- COVIDiSTRESS Global Survey Round II, CC-BY 4.0. Data file Final_COVIDiSTRESS_Vol2_cleaned.csv, obtained via osf.io/n26mb. Paper: Blackburn, Vestergren et al. (2022), Scientific Data 9, 331, doi:10.1038/s41597-022-01383-6 -->
<!-- full note: SOURCE CORRECTION, raised and applied under #1831 on 2026-09-04. The IRW dictionary attributed all eleven ecps_sahm_2024_* tables to Sahm, Burghart & Mier's German ERQ-S validation (doi:10.17605/OSF.IO/N26MB), describing each as a subscale of that instrument. That was wrong for ten of the eleven: the data are the COVIDiSTRESS Global Survey Round II, whose cleaned file is hosted in the ERQ-S OSF project alongside that study's own files, with that project's README pointing to osf.io/36tsd for the file's description -- which is how the mix-up arose. Every item code in all eleven tables is a column of that file, which rederive_ecps.py re-checks. The exception is ecps_sahm_2024_emotion, which genuinely is the ERQ short form: COVIDiSTRESS II included it as a block, and that is the subsample Sahm et al. validated. Item text is tied to item codes by the instrument's listing order within each block, with block item counts matching the live tables exactly. The moral values block lists 11 statements and the table has 11 items. OPTION LABELS COME FROM THE ADMINISTERED FORM, not the questionnaire workbooks. The two workbooks on OSF are registration-stage documents and their response designs do not describe what was administered: they give information acquisition as a 1-7 slider where the data run 1-10, and the agreement scales as 1-6 where the data run 1-7 or 0-6. 'Copy of survey.pdf', in the Round II project's own Final Data Set folder, is the Qualtrics form and prints every ladder verbatim. It settled two things the workbooks got wrong: the agreement ladder carries a NEUTRAL midpoint the workbooks omitted, so a six-anchor list is really seven options; and the compliance items are past tense. Every live resp level in all eight tables maps onto a position in its own item's ladder, with no level left over. The form is authoritative but its print view is partial -- it renders one statement per randomised misperception block and two rows of each norm matrix -- so 190 shipped strings verify against it and 16 against the workbooks, 0 against neither, which rederive_ecps.py reports per run. Positions the form left without a verbal label ship empty rather than invented. The _0neutral decode: the cleaned file carries each of these items twice, as *_0neutral and *_midneutral, and IRW uses the _0neutral copy. Cross-tabulating the pair recovers the scale exactly -- 32 of the 33 paired items give 0->4, 1->1, 2->2, 3->3, 4->5, 5->6, 6->7, so the midpoint is pulled out to 0 and the six named anchors stay at 1-6. The exception is the single willingness item, a 5-point scale giving 0->3, 1->1, 2->2, 3->4, 4->5. Re-run per item by rederive_ecps.py. So all 77 option cells are labelled: 0 = Neutral and 1-6 the six named anchors. The survey ran in 47 languages across 179 countries, so no single administered language exists to name; the shipped text is the English master form and the language column is omitted rather than given a value wrong for most respondents. Raised on #1831 as a case #1777 does not cover.  -->

```yaml
- table: ecps_sahm_2024_moral
  issue: |-
    Item text is the administered English wording. resp 0-6 is a 7-point agreement scale whose midpoint the cleaned data recodes to 0: 0 = Neutral, then 1 = Strongly disagree through 6 = Strongly agree. Note that 0 is the MIDDLE of the scale, not its bottom. Note on the table name: the `ecps_sahm_2024_` prefix points at Sahm et al.'s German Emotion Regulation Questionnaire validation, which is not what this table is. These responses are the COVIDiSTRESS Global Survey Round II; its cleaned data file is hosted inside that study's OSF project, which is how the two came to be conflated. The name is kept because renaming a published table breaks every existing reference to it.
```

## `ecps_sahm_2024_sscd` (batch_022)

<!-- mapping_basis=paper_order text_source=study_materials source=osf.io/36tsd 'Copy of survey.pdf' (the administered Qualtrics form, in the project's Final Data Set folder) with 'Measured Variables.xlsx'; osf.io/vxuf4 'MainStudy_CriticalMeasures.xlsx' -- COVIDiSTRESS Global Survey Round II, CC-BY 4.0. Data file Final_COVIDiSTRESS_Vol2_cleaned.csv, obtained via osf.io/n26mb. Paper: Blackburn, Vestergren et al. (2022), Scientific Data 9, 331, doi:10.1038/s41597-022-01383-6 -->
<!-- full note: SOURCE CORRECTION, raised and applied under #1831 on 2026-09-04. The IRW dictionary attributed all eleven ecps_sahm_2024_* tables to Sahm, Burghart & Mier's German ERQ-S validation (doi:10.17605/OSF.IO/N26MB), describing each as a subscale of that instrument. That was wrong for ten of the eleven: the data are the COVIDiSTRESS Global Survey Round II, whose cleaned file is hosted in the ERQ-S OSF project alongside that study's own files, with that project's README pointing to osf.io/36tsd for the file's description -- which is how the mix-up arose. Every item code in all eleven tables is a column of that file, which rederive_ecps.py re-checks. The exception is ecps_sahm_2024_emotion, which genuinely is the ERQ short form: COVIDiSTRESS II included it as a block, and that is the subsample Sahm et al. validated. Three blocks are represented as separate section_id values, and the table's own item_family column separates them 1/2/3 exactly as compliance / injunctive norms / descriptive norms. The three ask about the same 8 behaviours in the same order and the instrument words them differently for each, which is what assigns each block its wording. OPTION LABELS COME FROM THE ADMINISTERED FORM, not the questionnaire workbooks. The two workbooks on OSF are registration-stage documents and their response designs do not describe what was administered: they give information acquisition as a 1-7 slider where the data run 1-10, and the agreement scales as 1-6 where the data run 1-7 or 0-6. 'Copy of survey.pdf', in the Round II project's own Final Data Set folder, is the Qualtrics form and prints every ladder verbatim. It settled two things the workbooks got wrong: the agreement ladder carries a NEUTRAL midpoint the workbooks omitted, so a six-anchor list is really seven options; and the compliance items are past tense. Every live resp level in all eight tables maps onto a position in its own item's ladder, with no level left over. The form is authoritative but its print view is partial -- it renders one statement per randomised misperception block and two rows of each norm matrix -- so 190 shipped strings verify against it and 16 against the workbooks, 0 against neither, which rederive_ecps.py reports per run. Positions the form left without a verbal label ship empty rather than invented. Compliance therefore ships the administered past tense ('Washed your hands regularly'), not the workbooks' imperative. Its ladder is the 7-point agreement run, so its 56 cells are labelled; the two norm ladders print only their ends ('Not important at all' to 'Very important', 'Nearly nobody' to 'Nearly everybody'), so 32 of their 112 cells are labelled and 80 are the form's own unlabelled positions. The form's 'Not applicable' is a separate column in the source and is not a level of resp, which is why the earlier reading of level 7 as Not applicable was wrong. The survey ran in 47 languages across 179 countries, so no single administered language exists to name; the shipped text is the English master form and the language column is omitted rather than given a value wrong for most respondents. Raised on #1831 as a case #1777 does not cover.  -->

```yaml
- table: ecps_sahm_2024_sscd
  issue: |-
    Item text and ladders are the administered English wording; the compliance items are past tense as administered. item_family separates compliance (1) from injunctive norms (2) and descriptive norms (3), and the three blocks use different ladders, so resp is not comparable across them. The norm ladders label only their ends, so their intermediate positions carry no option_text. Item 7 of each block is reverse-worded ('Met with people outside of your household for non-essential reasons') and runs against the other seven. Note on the table name: the `ecps_sahm_2024_` prefix points at Sahm et al.'s German Emotion Regulation Questionnaire validation, which is not what this table is. These responses are the COVIDiSTRESS Global Survey Round II; its cleaned data file is hosted inside that study's OSF project, which is how the two came to be conflated. The name is kept because renaming a published table breaks every existing reference to it.
```

## `ecps_sahm_2024_stress` (batch_022)

<!-- mapping_basis=paper_order text_source=study_materials source=osf.io/36tsd 'Copy of survey.pdf' (the administered Qualtrics form, in the project's Final Data Set folder) with 'Measured Variables.xlsx'; osf.io/vxuf4 'MainStudy_CriticalMeasures.xlsx' -- COVIDiSTRESS Global Survey Round II, CC-BY 4.0. Data file Final_COVIDiSTRESS_Vol2_cleaned.csv, obtained via osf.io/n26mb. Paper: Blackburn, Vestergren et al. (2022), Scientific Data 9, 331, doi:10.1038/s41597-022-01383-6 -->
<!-- full note: SOURCE CORRECTION, raised and applied under #1831 on 2026-09-04. The IRW dictionary attributed all eleven ecps_sahm_2024_* tables to Sahm, Burghart & Mier's German ERQ-S validation (doi:10.17605/OSF.IO/N26MB), describing each as a subscale of that instrument. That was wrong for ten of the eleven: the data are the COVIDiSTRESS Global Survey Round II, whose cleaned file is hosted in the ERQ-S OSF project alongside that study's own files, with that project's README pointing to osf.io/36tsd for the file's description -- which is how the mix-up arose. Every item code in all eleven tables is a column of that file, which rederive_ecps.py re-checks. The exception is ecps_sahm_2024_emotion, which genuinely is the ERQ short form: COVIDiSTRESS II included it as a block, and that is the subsample Sahm et al. validated. Three blocks are represented as separate section_id values: the Perceived Stress Scale (10 items), primary stressors (4) and secondary stressors (14). OPTION LABELS COME FROM THE ADMINISTERED FORM, not the questionnaire workbooks. The two workbooks on OSF are registration-stage documents and their response designs do not describe what was administered: they give information acquisition as a 1-7 slider where the data run 1-10, and the agreement scales as 1-6 where the data run 1-7 or 0-6. 'Copy of survey.pdf', in the Round II project's own Final Data Set folder, is the Qualtrics form and prints every ladder verbatim. It settled two things the workbooks got wrong: the agreement ladder carries a NEUTRAL midpoint the workbooks omitted, so a six-anchor list is really seven options; and the compliance items are past tense. Every live resp level in all eight tables maps onto a position in its own item's ladder, with no level left over. The form is authoritative but its print view is partial -- it renders one statement per randomised misperception block and two rows of each norm matrix -- so 190 shipped strings verify against it and 16 against the workbooks, 0 against neither, which rederive_ecps.py reports per run. Positions the form left without a verbal label ship empty rather than invented. The PSS items appear in their canonical order and the stressor blocks on a 5-point concern ladder, so all 137 option cells are labelled. This table was held out of the batch because the workbooks' secondary-stressor order is contradicted by the data; the administered form gives a different order, putting 'Receiving inadequate, unclear, or conflicting information' before 'Not having enough support from close others', and that order fits the conditional-block structure at 12 of the 14 positions -- indices 1-4 general at about n=10,000 each, 5 relationship at 6,607, 6-7 occupation at 9,905 and 9,623, 10-11 student at about 1,140, 12-13 children at about 3,200 and 14 others at 8,359. The two that do not fit are indices 8 and 9, 'Being unable to work from home' and 'Sharing a workspace with others', which carry only 6 and 8 responses while their own block-mates carry about 9,700. No ordering can produce that, so it is a source-side display problem rather than a mapping error -- note that 'Sharing a workspace with others' is printed twice in the form, in the occupation and student blocks. The survey ran in 47 languages across 179 countries, so no single administered language exists to name; the shipped text is the English master form and the language column is omitted rather than given a value wrong for most respondents. Raised on #1831 as a case #1777 does not cover.  -->

```yaml
- table: ecps_sahm_2024_stress
  issue: |-
    Item text and ladders are the administered English wording. The table pools three blocks with two ladders: the Perceived Stress Scale on 0 = Never to 4 = Very often, and both stressor blocks on 0 = Not at all concerned to 4 = Very concerned. Two secondary-stressor items, indices 8 and 9, carry only 6 and 8 responses against about 9,700 for the rest of their block, which is a defect in the source's display logic rather than in the item text. Note on the table name: the `ecps_sahm_2024_` prefix points at Sahm et al.'s German Emotion Regulation Questionnaire validation, which is not what this table is. These responses are the COVIDiSTRESS Global Survey Round II; its cleaned data file is hosted inside that study's OSF project, which is how the two came to be conflated. The name is kept because renaming a published table breaks every existing reference to it.
```

## `ecps_sahm_2024_vaccine` (batch_022)

<!-- mapping_basis=paper_order text_source=study_materials source=osf.io/36tsd 'Copy of survey.pdf' (the administered Qualtrics form, in the project's Final Data Set folder) with 'Measured Variables.xlsx'; osf.io/vxuf4 'MainStudy_CriticalMeasures.xlsx' -- COVIDiSTRESS Global Survey Round II, CC-BY 4.0. Data file Final_COVIDiSTRESS_Vol2_cleaned.csv, obtained via osf.io/n26mb. Paper: Blackburn, Vestergren et al. (2022), Scientific Data 9, 331, doi:10.1038/s41597-022-01383-6 -->
<!-- full note: SOURCE CORRECTION, raised and applied under #1831 on 2026-09-04. The IRW dictionary attributed all eleven ecps_sahm_2024_* tables to Sahm, Burghart & Mier's German ERQ-S validation (doi:10.17605/OSF.IO/N26MB), describing each as a subscale of that instrument. That was wrong for ten of the eleven: the data are the COVIDiSTRESS Global Survey Round II, whose cleaned file is hosted in the ERQ-S OSF project alongside that study's own files, with that project's README pointing to osf.io/36tsd for the file's description -- which is how the mix-up arose. Every item code in all eleven tables is a column of that file, which rederive_ecps.py re-checks. The exception is ecps_sahm_2024_emotion, which genuinely is the ERQ short form: COVIDiSTRESS II included it as a block, and that is the subsample Sahm et al. validated. Two blocks are represented as separate section_id values: a single willingness item and 6 vaccine attitude items. OPTION LABELS COME FROM THE ADMINISTERED FORM, not the questionnaire workbooks. The two workbooks on OSF are registration-stage documents and their response designs do not describe what was administered: they give information acquisition as a 1-7 slider where the data run 1-10, and the agreement scales as 1-6 where the data run 1-7 or 0-6. 'Copy of survey.pdf', in the Round II project's own Final Data Set folder, is the Qualtrics form and prints every ladder verbatim. It settled two things the workbooks got wrong: the agreement ladder carries a NEUTRAL midpoint the workbooks omitted, so a six-anchor list is really seven options; and the compliance items are past tense. Every live resp level in all eight tables maps onto a position in its own item's ladder, with no level left over. The form is authoritative but its print view is partial -- it renders one statement per randomised misperception block and two rows of each norm matrix -- so 190 shipped strings verify against it and 16 against the workbooks, 0 against neither, which rederive_ecps.py reports per run. Positions the form left without a verbal label ship empty rather than invented. The _0neutral decode: the cleaned file carries each of these items twice, as *_0neutral and *_midneutral, and IRW uses the _0neutral copy. Cross-tabulating the pair recovers the scale exactly -- 32 of the 33 paired items give 0->4, 1->1, 2->2, 3->3, 4->5, 5->6, 6->7, so the midpoint is pulled out to 0 and the six named anchors stay at 1-6. The exception is the single willingness item, a 5-point scale giving 0->3, 1->1, 2->2, 3->4, 4->5. Re-run per item by rederive_ecps.py. This table was held out of the batch because its 6 attitude codes are numbered 2, 3, 4, 5, 6 and 9, so position 1 and two others are absent from the cleaned file and the codes cannot be read as 1-6. The administered form lists exactly 6 attitude statements, so ascending code order is matched to printed order, which is an order inference and the reason this stays PARTIAL. The willingness item is unambiguous and ships its administered wording, including the form's own typo 'how wiling you were'. Its slider labels only its ends and midpoint, so 2 of the 47 option cells are the form's unlabelled positions. The survey ran in 47 languages across 179 countries, so no single administered language exists to name; the shipped text is the English master form and the language column is omitted rather than given a value wrong for most respondents. Raised on #1831 as a case #1777 does not cover.  -->

```yaml
- table: ecps_sahm_2024_vaccine
  issue: |-
    Item text and ladders are the administered English wording, including the form's typo 'how wiling you were' in the willingness item. The table pools two scales: willingness on resp 0-4, where 0 is the Neutral midpoint, 1 = 'Not willing at all' and 4 = 'Very willing' with two unlabelled positions between; and 6 attitude items on resp 0-6, where 0 is the Neutral midpoint and 1-6 run Strongly disagree to Strongly agree. The attitude codes are numbered 2-6 and 9, so three of the instrument's attitude positions are absent from the data. Note on the table name: the `ecps_sahm_2024_` prefix points at Sahm et al.'s German Emotion Regulation Questionnaire validation, which is not what this table is. These responses are the COVIDiSTRESS Global Survey Round II; its cleaned data file is hosted inside that study's OSF project, which is how the two came to be conflated. The name is kept because renaming a published table breaks every existing reference to it.
```

## `ecps_sahm_2024_emotion` (batch_022)

<!-- mapping_basis=paper_order text_source=study_materials source=osf.io/36tsd 'Copy of survey.pdf' (the administered Qualtrics form, in the project's Final Data Set folder) with 'Measured Variables.xlsx'; osf.io/vxuf4 'MainStudy_CriticalMeasures.xlsx' -- COVIDiSTRESS Global Survey Round II, CC-BY 4.0. Data file Final_COVIDiSTRESS_Vol2_cleaned.csv, obtained via osf.io/n26mb. Paper: Blackburn, Vestergren et al. (2022), Scientific Data 9, 331, doi:10.1038/s41597-022-01383-6 -->
<!-- full note: SOURCE CORRECTION, raised and applied under #1831 on 2026-09-04. The IRW dictionary attributed all eleven ecps_sahm_2024_* tables to Sahm, Burghart & Mier's German ERQ-S validation (doi:10.17605/OSF.IO/N26MB), describing each as a subscale of that instrument. That was wrong for ten of the eleven: the data are the COVIDiSTRESS Global Survey Round II, whose cleaned file is hosted in the ERQ-S OSF project alongside that study's own files, with that project's README pointing to osf.io/36tsd for the file's description -- which is how the mix-up arose. Every item code in all eleven tables is a column of that file, which rederive_ecps.py re-checks. THIS IS THE EXCEPTION, which genuinely is the ERQ short form: COVIDiSTRESS II included it as a block, and that is the subsample Sahm et al. validated. Item text is tied to item codes by the instrument's listing order within each block, with block item counts matching the live tables exactly. The identity block lists 4 statements and the table has 4 items. The wording is corroborated by a second document: the block appears in both workbooks and their 5 lines agree word for word. So for this table the dictionary's original description was substantively right about the instrument, if not about the study or the language: these 8 items are the ERQ short form, 4 suppression and 4 reappraisal. OPTION LABELS COME FROM THE ADMINISTERED FORM, not the questionnaire workbooks. The two workbooks on OSF are registration-stage documents and their response designs do not describe what was administered: they give information acquisition as a 1-7 slider where the data run 1-10, and the agreement scales as 1-6 where the data run 1-7 or 0-6. 'Copy of survey.pdf', in the Round II project's own Final Data Set folder, is the Qualtrics form and prints every ladder verbatim. It settled two things the workbooks got wrong: the agreement ladder carries a NEUTRAL midpoint the workbooks omitted, so a six-anchor list is really seven options; and the compliance items are past tense. Every live resp level in all eight tables maps onto a position in its own item's ladder, with no level left over. The form is authoritative but its print view is partial -- it renders one statement per randomised misperception block and two rows of each norm matrix -- so 190 shipped strings verify against it and 16 against the workbooks, 0 against neither, which rederive_ecps.py reports per run. Positions the form left without a verbal label ship empty rather than invented. The _0neutral decode: the cleaned file carries each of these items twice, as *_0neutral and *_midneutral, and IRW uses the _0neutral copy. Cross-tabulating the pair recovers the scale exactly -- 32 of the 33 paired items give 0->4, 1->1, 2->2, 3->3, 4->5, 5->6, 6->7, so the midpoint is pulled out to 0 and the six named anchors stay at 1-6. The exception is the single willingness item, a 5-point scale giving 0->3, 1->1, 2->2, 3->4, 4->5. Re-run per item by rederive_ecps.py. The emotions block prints only its ends and midpoint rather than all seven labels, so 24 of the 56 option cells are labelled and 32 are the form's own unlabelled positions. ITEM ORDER IS THE ADMINISTERED ONE, NOT THE WORKBOOKS'. The two disagree about where 'When I am feeling negative emotions, I make sure not to express them' sits -- 4th in the form, 7th in the workbooks -- and the data settle it decisively. Under the form's order the ERQ's suppression/reappraisal split falls on items 1,2,4,5 against 3,6,7,8 with a within-minus-between correlation gap of 0.268, which is the maximum of 2000 random 4/4 partitions; under the workbook order the same split gives 0.003, the 65th percentile, i.e. no better than chance. The two clusters are visible directly in the matrix: 1,2,4,5 intercorrelate 0.24 to 0.41, 3,6,7,8 intercorrelate 0.31 to 0.60, and cross-cluster pairs run 0.06 to 0.21. The survey ran in 47 languages across 179 countries, so no single administered language exists to name; the shipped text is the English master form and the language column is omitted rather than given a value wrong for most respondents. Raised on #1831 as a case #1777 does not cover.  -->

```yaml
- table: ecps_sahm_2024_emotion
  issue: |-
    The Emotion Regulation Questionnaire short form, 8 items: expressive suppression (items 1, 2, 4 and 5) and cognitive reappraisal (3, 6, 7 and 8). resp 0-6 is a 7-point agreement scale whose midpoint the cleaned data recodes to 0, so 0 = Neutral, 1 = Strongly disagree and 6 = Strongly agree; the form labelled only those three positions, so the four between them carry no option_text. Unlike its ten sibling tables this one really is the Emotion Regulation Questionnaire the dataset description names.
```

## `ecps_sahm_2024_support` (batch_022)

<!-- mapping_basis=paper_order text_source=study_materials source=osf.io/36tsd 'Copy of survey.pdf' (the administered Qualtrics form, in the project's Final Data Set folder) with 'Measured Variables.xlsx'; osf.io/vxuf4 'MainStudy_CriticalMeasures.xlsx' -- COVIDiSTRESS Global Survey Round II, CC-BY 4.0. Data file Final_COVIDiSTRESS_Vol2_cleaned.csv, obtained via osf.io/n26mb. Paper: Blackburn, Vestergren et al. (2022), Scientific Data 9, 331, doi:10.1038/s41597-022-01383-6 -->
<!-- full note: SOURCE CORRECTION, raised and applied under #1831 on 2026-09-04. The IRW dictionary attributed all eleven ecps_sahm_2024_* tables to Sahm, Burghart & Mier's German ERQ-S validation (doi:10.17605/OSF.IO/N26MB), describing each as a subscale of that instrument. That was wrong for ten of the eleven: the data are the COVIDiSTRESS Global Survey Round II, whose cleaned file is hosted in the ERQ-S OSF project alongside that study's own files, with that project's README pointing to osf.io/36tsd for the file's description -- which is how the mix-up arose. Every item code in all eleven tables is a column of that file, which rederive_ecps.py re-checks. The exception is ecps_sahm_2024_emotion, which genuinely is the ERQ short form: COVIDiSTRESS II included it as a block, and that is the subsample Sahm et al. validated. Item text is tied to item codes by the instrument's listing order within each block, with block item counts matching the live tables exactly. The identity block lists 4 statements and the table has 4 items. The wording is corroborated by a second document: the block appears in both workbooks and their 5 lines agree word for word. OPTION LABELS COME FROM THE ADMINISTERED FORM, not the questionnaire workbooks. The two workbooks on OSF are registration-stage documents and their response designs do not describe what was administered: they give information acquisition as a 1-7 slider where the data run 1-10, and the agreement scales as 1-6 where the data run 1-7 or 0-6. 'Copy of survey.pdf', in the Round II project's own Final Data Set folder, is the Qualtrics form and prints every ladder verbatim. It settled two things the workbooks got wrong: the agreement ladder carries a NEUTRAL midpoint the workbooks omitted, so a six-anchor list is really seven options; and the compliance items are past tense. Every live resp level in all eight tables maps onto a position in its own item's ladder, with no level left over. The form is authoritative but its print view is partial -- it renders one statement per randomised misperception block and two rows of each norm matrix -- so 190 shipped strings verify against it and 16 against the workbooks, 0 against neither, which rederive_ecps.py reports per run. Positions the form left without a verbal label ship empty rather than invented. The _0neutral decode: the cleaned file carries each of these items twice, as *_0neutral and *_midneutral, and IRW uses the _0neutral copy. Cross-tabulating the pair recovers the scale exactly -- 32 of the 33 paired items give 0->4, 1->1, 2->2, 3->3, 4->5, 5->6, 6->7, so the midpoint is pulled out to 0 and the six named anchors stay at 1-6. The exception is the single willingness item, a 5-point scale giving 0->3, 1->1, 2->2, 3->4, 4->5. Re-run per item by rederive_ecps.py. The support block prints all seven labels, so all 21 option cells are labelled: 0 = Neutral and 1-6 the six named anchors. The survey ran in 47 languages across 179 countries, so no single administered language exists to name; the shipped text is the English master form and the language column is omitted rather than given a value wrong for most respondents. Raised on #1831 as a case #1777 does not cover.  -->

```yaml
- table: ecps_sahm_2024_support
  issue: |-
    Three perceived-social-support items. resp 0-6 is a 7-point agreement scale whose midpoint the cleaned data recodes to 0: 0 = Neutral, then 1 = Strongly disagree through 6 = Strongly agree. Note that 0 is the MIDDLE of the scale, not its bottom. Note on the table name: the `ecps_sahm_2024_` prefix points at Sahm et al.'s German Emotion Regulation Questionnaire validation, which is not what this table is. These responses are the COVIDiSTRESS Global Survey Round II; its cleaned data file is hosted inside that study's OSF project, which is how the two came to be conflated. The name is kept because renaming a published table breaks every existing reference to it.
```

## `ecps_sahm_2024_trust` (batch_022)

<!-- mapping_basis=paper_order text_source=study_materials source=osf.io/36tsd 'Copy of survey.pdf' (the administered Qualtrics form, in the project's Final Data Set folder) with 'Measured Variables.xlsx'; osf.io/vxuf4 'MainStudy_CriticalMeasures.xlsx' -- COVIDiSTRESS Global Survey Round II, CC-BY 4.0. Data file Final_COVIDiSTRESS_Vol2_cleaned.csv, obtained via osf.io/n26mb. Paper: Blackburn, Vestergren et al. (2022), Scientific Data 9, 331, doi:10.1038/s41597-022-01383-6 -->
<!-- full note: SOURCE CORRECTION, raised and applied under #1831 on 2026-09-04. The IRW dictionary attributed all eleven ecps_sahm_2024_* tables to Sahm, Burghart & Mier's German ERQ-S validation (doi:10.17605/OSF.IO/N26MB), describing each as a subscale of that instrument. That was wrong for ten of the eleven: the data are the COVIDiSTRESS Global Survey Round II, whose cleaned file is hosted in the ERQ-S OSF project alongside that study's own files, with that project's README pointing to osf.io/36tsd for the file's description -- which is how the mix-up arose. Every item code in all eleven tables is a column of that file, which rederive_ecps.py re-checks. The exception is ecps_sahm_2024_emotion, which genuinely is the ERQ short form: COVIDiSTRESS II included it as a block, and that is the subsample Sahm et al. validated. Item text is tied to item codes by the instrument's listing order within each block, with block item counts matching the live tables exactly. The identity block lists 4 statements and the table has 4 items. The wording is corroborated by a second document: the block appears in both workbooks and their 5 lines agree word for word. OPTION LABELS COME FROM THE ADMINISTERED FORM, not the questionnaire workbooks. The two workbooks on OSF are registration-stage documents and their response designs do not describe what was administered: they give information acquisition as a 1-7 slider where the data run 1-10, and the agreement scales as 1-6 where the data run 1-7 or 0-6. 'Copy of survey.pdf', in the Round II project's own Final Data Set folder, is the Qualtrics form and prints every ladder verbatim. It settled two things the workbooks got wrong: the agreement ladder carries a NEUTRAL midpoint the workbooks omitted, so a six-anchor list is really seven options; and the compliance items are past tense. Every live resp level in all eight tables maps onto a position in its own item's ladder, with no level left over. The form is authoritative but its print view is partial -- it renders one statement per randomised misperception block and two rows of each norm matrix -- so 190 shipped strings verify against it and 16 against the workbooks, 0 against neither, which rederive_ecps.py reports per run. Positions the form left without a verbal label ship empty rather than invented. Trust is the one block here that is not an agreement scale: the form prints it as a percentage slider running 'No trust' then 10% through 90% in ten-point steps, which is exactly the live 0-10 range, 11 positions. The top position's label is not in the PDF's text layer, so resp 10 ships empty rather than inferred as 100%; 70 of the 77 option cells are labelled. This block has no _0neutral twin in the cleaned file and needs none, its codes being plain trust_1 to trust_7. The survey ran in 47 languages across 179 countries, so no single administered language exists to name; the shipped text is the English master form and the language column is omitted rather than given a value wrong for most respondents. Raised on #1831 as a case #1777 does not cover.  -->

```yaml
- table: ecps_sahm_2024_trust
  issue: |-
    Seven institutional-trust items. resp 0-10 is a percentage slider in ten-point steps: 0 = 'No trust', then 10% at resp 1 through 90% at resp 9. The form's label for the top position is not recoverable from the deposited PDF, so resp 10 carries no option_text, though the arithmetic of the scale makes it 100%. Note on the table name: the `ecps_sahm_2024_` prefix points at Sahm et al.'s German Emotion Regulation Questionnaire validation, which is not what this table is. These responses are the COVIDiSTRESS Global Survey Round II; its cleaned data file is hosted inside that study's OSF project, which is how the two came to be conflated. The name is kept because renaming a published table breaks every existing reference to it.
```

## `conspiracy_asd__cognitive_flexibility` (batch_023)

<!-- mapping_basis=data_labels text_source=study_materials source=https://doi.org/10.6084/m9.figshare.30903575.v2 (CC BY 4.0); "Plain text version of survey.docx" + Qualtrics header rows of "Questionnaire 1 (Conspiracy Beliefs)_July 16 2024_08.34.xlsx" -->
<!-- full note: 20-item Cognitive Flexibility Inventory. The deposit's Qualtrics export labels each column in header row 1 (e.g. 'Cognitive flexibility - I am good at ''sizing up'' situations') and header row 0 carries the codes ._1..._20; data/conspiracy_belief_schizotypy_asd.py assigns cfi_1..cfi_20 by rank over exactly those 20 columns (raw indices 32-51 bounded by the THINKING STYLES TOTAL and COGNITIVE FLEXIBILITY TOTALS columns) so rank == label number. Item wording transcribed from the deposit's plain-text survey docx rather than those labels because the processed xlsx's label row has a global find/replace artifact that turned every 'no' into '2' ('k2w'=know, '2t'=not, 'a2ther'=another); the deposit's RAW DATA xlsx carries the same labels uncorrupted and matches the docx word for word. Only deviation from literal transcription: the U+FB01 'fi' ligature in the docx (difﬁcult, ﬁnd) was expanded to 'fi'. Response anchors 1=Strongly disagree .. 7=Strongly agree taken from the docx's own scale table printed under the Cognitive Flexibility heading. Per-item n in the raw file is 433 for all 20 columns, matching irw_table_sets() per-item n exactly. -->

```yaml
- table: conspiracy_asd__cognitive_flexibility
  issue: |-
    Item wording here comes from the deposit's plain-text survey document, not from the coded Qualtrics workbook that accompanies it. That workbook's label row is corrupted by a global find/replace of 'no' to '2' -- it reads 'I feel I have 2 power' and 'I just don't k2w what to do' -- so anyone comparing this table against those labels will find differences. The plain-text document agrees word for word with the uncorrupted labels in the deposit's raw data file.
```

## `conspiracy_asd__conspiracy_gcbs` (batch_023)

<!-- mapping_basis=data_labels text_source=study_materials source=https://doi.org/10.6084/m9.figshare.30903575.v2 (raw Excel header row; survey .docx Table 6) -->
<!-- full note: Item codes gcbs_1..gcbs_15 are assigned positionally by data/conspiracy_belief_schizotypy_asd.py (SCALES entry range(64,79), prefix 'gcbs', enumerate start=1) over the raw Figshare Excel. That positional range was diffed against the file's own header row: columns 64-78 (0-based) are exactly the 15 cells labelled 'Conspiracy Beliefs - <item>', bracketed by 'ASD TRAITS TOTALS' at 63 and 'CONSPIRACY BELIEFS TOTALS' at 79, so gcbs_N is the Nth GCBS column and its text is the label at that position. Words shipped from that same header row with the file's 'no'->'2' corruption reversed; instructions and the endpoint anchors from the study's survey .docx (Table 6), which reproduces the same 15 items in the same order. -->

```yaml
- table: conspiracy_asd__conspiracy_gcbs
  issue: |-
    The source Excel's question-text header carries a mechanical corruption in which the letters 'no' were replaced by the digit '2' (e.g. 'in2cent', 'tech2logy'); the item text published here reverses that substitution.
```

## `conspiracy_asd__schizotypy` (batch_023)

<!-- mapping_basis=data_labels text_source=study_materials source=Figshare 10.6084/m9.figshare.30903575.v2 (CC BY 4.0): item text and instructions from "Plain text version of survey.docx"; item code mapping from the header rows of "RAW DATA Questionnaire 1 (Conspiracy Beliefs)_June 4, 2024" and "Questionnaire 1 (Conspiracy Beliefs)_July 16, 2024" (columns ._1 .. ._37, "Schizotypy - <item text>") -->
<!-- full note: Mapping is data-file labels: data/conspiracy_belief_schizotypy_asd.py assigns schizotypy_1..37 positionally over coded-workbook columns 80-116, whose own header numbering runs ._1 .. ._37 with the item text inline, so rank and label number coincide 1:1. Diffed all 37 shipped item_text strings against the source header at that position: identical apart from comma/period punctuation. Words shipped from the survey docx rather than the coded header because the coded header row carries a find-and-replace defect (every "no" rendered "2"); the RAW Qualtrics export header is uncorrupted and matches the docx. "Schizotypy - " section prefix stripped from the header strings when diffing. Response anchors from the 5-point table printed under the schizotypy instructions in the docx. Instructions transcribed verbatim including their "from 1 to 4", which contradicts that table and the 1-5 data. -->

```yaml
- table: conspiracy_asd__schizotypy
  issue: |-
    The item codes are a 37-item sequence, not MSS-B item numbers: the study administered all 38 MSS-B items but its deposited data omit "I believe that dreams have magical properties" (survey item 11), so schizotypy_11 onward are offset by one from the published MSS-B numbering. The transcribed instructions also say to answer "from 1 to 4" while the anchor table beneath them and the data are 1-5.
```

## `conspiracy_asd__thinking_styles` (batch_023)

<!-- mapping_basis=data_labels text_source=study_materials source=Figshare 10.6084/m9.figshare.30903575.v2 (CC BY 4.0): "Plain text version of survey.docx" (Thinking Styles item wording, instruction line, and the 1-5 anchor legend table) and the coded workbook's row 1 (column-level 'Thinking Styles - ...' labels, used for column identity only) -->
<!-- full note: Item codes ts_1..ts_10 are assigned POSITIONALLY by data/conspiracy_belief_schizotypy_asd.py (ts_N <- raw workbook 0-based column 21+N-1), so the positional claim was checked rather than assumed: each source column's mean, floor% and ceiling% reproduce the live IRW per-item values exactly (largest mean deviation 4.6e-07, attributable to rounding the hard-coded source means to 6 d.p.; floor/ceiling identical to 1e-04 pp). Words taken from the deposit's survey.docx, not from the workbook header row, because the latter carries a find/replace corruption ('k2w' for 'know'). Leading item numbering stripped. Response anchors come from the survey's own instruction line and its 1-5 anchor legend table; midpoints 2-4 are unlabelled in the source and shipped blank. Rights: the source IRW copied from is CC BY 4.0 and no non-commercial statement by the REI's rights holder could be quoted, so no wording_rights flag is set. -->

```yaml
- table: conspiracy_asd__thinking_styles
  issue: |-
    The instrument is the 10-item Rational-Experiential Inventory short form, administered under the survey's own heading 'Thinking Styles'. Only the two extreme scale points are labelled on the instrument ('completely false' and 'completely true'), so `option_text` is blank for responses 2, 3 and 4 -- those points are genuinely unlabelled rather than missing. Wording is taken from the deposit's plain-text survey document, because the coded workbook's labels carry a find/replace artifact ('k2w' for 'know').
```

## `cooper_2018_funny_topics` (batch_023)

<!-- mapping_basis=data_labels text_source=study_materials source=PLOS ONE 10.1371/journal.pone.0201258 -- S3 File (survey instrument, .docx) for the wording; S5 File (S3 File data, .xlsx) for the column headers; S1 File (S1 Table) for the verification statistics -->
<!-- full note: The IRW item codes ARE the S5 File .xlsx column headers verbatim (data/cooper_2018_humor.py melts the 34 funny.* columns with no renaming), and each header names its joke subject, so code->subject needs no inference. The wording shipped in item_text is the survey's own literal list from the S3 File ('jokes about African Americans', 'food puns', ...), trailing whitespace stripped, matched to the codes by subject name. instructions concatenates the two consecutive survey paragraphs that frame this checklist (the shared funny/offensive preamble and the funny-specific select-all stem), verbatim. option_text is NOT verbatim source text: this is a check-all-that-apply battery and the source publishes no response labels, so 1='Selected' / 0='Not selected' describes the 0/1 coding implied by 'Please select all that you might find funny.' Mapping additionally checked against S1 Table (see verification sidecar); the sibling table cooper_2018_offensive_topics is the same 34 subjects under the offensive stem. -->

```yaml
- table: cooper_2018_funny_topics
  issue: |-
    This battery is a select-all-that-apply checklist, so `resp` is a checkbox state rather than a chosen response option, and the `option_text` values 'Selected' and 'Not selected' describe that 0/1 coding rather than transcribing anything the survey printed -- the source publishes no response labels for this battery, only the stem 'Please select all that you might find funny.' Item text and instructions are verbatim from the study's own survey file.
```

## `cooper_2018_offensive_topics` (batch_023)

<!-- mapping_basis=data_labels text_source=study_materials source=PLOS ONE 10.1371/journal.pone.0201258, S3 File (survey instrument, .docx) and S5 File (S3 Data, .xlsx response file); CC BY 4.0 -->
<!-- full note: Item codes are the S5 xlsx's own column headers (data/cooper_2018_humor.py melts them unchanged), and each header names its own topic (offensive.muslims, offensive.food puns), so code-to-text is fixed at the source with no inference; all 34 headers map one-to-one onto the 34 topics listed under the offensive checklist in S3 File. item_text is the S3 File topic wording verbatim (e.g. 'jokes about people with disabilities' for offensive.disabilities, 'jokes about farts or poop' for offensive.farts, 'food puns' for offensive.food puns), including the offensive/political/identity-based topics as published. instructions concatenates the two consecutive spans the respondent read before this checklist (the shared funny/offensive framing paragraph and the offensive-checklist question stem); the paragraph break between them was flattened to a space, no words changed. This is a check-all-that-apply list, so the survey prints no labels for the 0/1 coding: option_text 'Selected'/'Not selected' describes the checkbox state and is not source wording. -->

```yaml
- table: cooper_2018_offensive_topics
  issue: |-
    This battery is a select-all-that-apply checklist, so `resp` is a checkbox state rather than a chosen response option, and the `option_text` values 'Selected' and 'Not selected' describe that 0/1 coding rather than transcribing survey wording -- the source prints no response labels for it. Item text is verbatim from the study's own survey file.
```

## `cormier_2024_personality` (batch_023)

<!-- mapping_basis=data_labels text_source=study_materials source=https://doi.org/10.1371/journal.pone.0304428.s002 (S2 Qualtrics export, row 1 question labels); PLOS ONE 19(6):e0304428, CC BY 4.0 -->
<!-- full note: IRW item codes ARE the Qualtrics column names (data/cormier_2024_hearing_battery.py melts Q20_1..Q20_16 unrenamed), and each column's question label in the export's header row carries its own item text -- no positional or order inference on the item axis. Deviations from literal transcription: the matrix prompt shared by all 16 rows was moved to `instructions` and the per-row remainder kept as `item_text`; newlines inside the label collapsed to single spaces. Option labels are the raw stored values themselves; their 1-5 coding comes from RESP_MAP in the processing script and was checked by route 9 (all 80 item x level counts match). Q20_8 is an attention check, left with a blank correct_response rather than shipping a key the schema would present as a scoring key. Wording rights: copied from a CC BY 4.0 PLOS deposit, and no NC clause for the BFI-2-XS could be quoted (the rights holder's page at colby.edu is Cloudflare-blocked), so no wording_rights flag is set. -->

```yaml
- table: cormier_2024_personality
  issue: |-
    Item Q20_8 is not a personality item: it is an attention check whose text instructs respondents to select 'Agree Strongly', and every respondent did (all 898 responses are 5). Exclude it before scoring the BFI-2-XS.
```

## `cormier_2024_phq4` (batch_023)

<!-- mapping_basis=data_labels text_source=study_materials source=PLOS ONE 10.1371/journal.pone.0304428 S2 File (https://doi.org/10.1371/journal.pone.0304428.s002), Qualtrics export; row 1 carries the verbatim question text per column -->
<!-- full note: Item codes Q22_1-Q22_4 ARE the source column names (data/cormier_2024_hearing_battery.py melts them unchanged), and the Qualtrics export's question-text header row gives each column's wording directly -- no inference on the item axis. Shipped item_text is the portion after the Qualtrics ' - ' separator; the shared block stem before it is recorded once in instructions. Instrument confirmed as a true 4-item PHQ-4 (GAD-2 items Q22_1/Q22_2 + PHQ-2 items Q22_3/Q22_4), matching the file's own SC10='PHQ-4', SC11='Anxiety', SC12='Depression' subscores; not a PHQ-9. option_text/resp taken from the raw label->integer map in data/cormier_2024_hearing_battery.py (MAP_Q22: Not at all=0 .. Nearly every day=3), the standard PHQ 0-3 coding; all four labels occur for all four items in the raw file and per-item n=898 x 4 items = 3592 = live n_rows. Rights: source is CC BY (PLOS); the PHQ family carries no non-commercial clause, so no wording_rights flag. -->

```yaml
- table: cormier_2024_phq4
  issue: |-
    The source Qualtrics export concatenates a shared block stem with each item stem in one header, joined by ' - '. That shared stem is shipped once in `instructions` and `item_text` holds only the per-item sentence, so an item here is shorter than the corresponding column header in the source file.
```

## `cormier_2024_pss4` (batch_023)

<!-- mapping_basis=data_labels text_source=study_materials source=Cormier K, Brennan C, Sharma A (2024) PLOS ONE 19(6):e0304428, doi:10.1371/journal.pone.0304428 (CC BY 4.0); S2 File raw Qualtrics export doi:10.1371/journal.pone.0304428.s002 (cached .cache/cormier_2024_pss4/s002.csv); processing script data/cormier_2024_hearing_battery.py -->
<!-- full note: mapping_basis=data_labels: row 1 of the Qualtrics export s002.csv carries the full question text for each of Q21_1..Q21_4, and the IRW codes ARE those source column names (data/cormier_2024_hearing_battery.py melts them unchanged), so the code->text tie is at the source with no inference; Step 5b exemption applies and no verification sidecar is owed. Corroboration recorded anyway: per-item raw label counts sum to 898 for all four items, equal to irw_table_sets() per-item n=898. text_source=study_materials -- item_text is the literal Qualtrics wording, with the shared stem prefix split off into `instructions` (identical across all four items) and only the post-' - ' clause kept as item_text; nothing was normalised to canonical PSS phrasing. option_text/resp come from the study's own labels via the script's MAP_Q21 (Never=0 ... Very often=4), applied without reversal, so Q21_2 and Q21_3 (the PSS reverse-keyed items) are stored raw. section_prompt blank, single trivial section_id, correct_response blank (no correct answer). RIGHTS: source is CC BY 4.0 PLOS; Cohen's PSS page states no non-commercial restriction (permission requests via ePROVIDE, 'completely free of charge'), so no quotable NC clause and no wording_rights column. -->

```yaml
- table: cormier_2024_pss4
  issue: |-
    Responses are stored raw rather than reverse-scored, so for the two PSS reverse-keyed items (Q21_2 'confident about your ability to handle your personal problems' and Q21_3 'things were going your way') a higher value means less perceived stress.
```

---

## REVIEW THESE TOO -- no draft generated

These tables shipped but earned no draft callout: their provenance carries no
`public_note` and its structured fields look clean. That is NOT the same as
having no caveat -- this script cannot see anything recorded only in
`notes.csv`, and three batch_009 tables were missed exactly that way. Read each
note below and decide; if it warrants a callout, write one by hand.

- **`cormier_2024_cognitive_decline`** (batch_023)
    - (no notes.csv entry either)

---

Skipped 2 tables with a provenance row but no shipped CSV (blocked at
extraction): `contreras_valdez_2022_bsq`, `contreras_valdez_2022_rses`

