# Regenerable API responses (~5 MB, goes stale). Delete to force a clean re-resolve.
doi_cache/
# Intermediate + snapshot inputs, regenerated by run_audit.py / re-fetched from the sheet.
audit_*.csv
dict_snapshot_*.csv
__pycache__/
